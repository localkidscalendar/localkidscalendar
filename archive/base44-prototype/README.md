# Base44 prototype (archived)

This folder is a **read-only archive** of the original Base44 export (entity schemas, Deno functions, and config). It is **not** used by the live site.

**Live stack:** React (Vite) on Vercel, Supabase (auth + Postgres), and serverless routes under `/api/*`.

The Base44 prototype deployment has been disabled. Do not add new features here; refer to `src/`, `api/`, and `supabase/` instead.
